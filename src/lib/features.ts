// Feature flags. Flip these to true to re-enable UI without touching
// downstream code — the routing, cart store, and page markup stay wired.

/** Show the cart icon in the header, Add-to-Cart buttons, etc. */
export const CART_ENABLED = false;
